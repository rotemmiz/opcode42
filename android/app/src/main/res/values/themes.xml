<resources>
    <!-- Splash/window theme — the real M3 theme is applied in Compose via Opcode42Theme.
         windowBackground follows the system day/night background so the cold-start splash
         matches the chosen scheme (no black flash in light theme). System-bar colors are
         transparent so enableEdgeToEdge() can draw content behind them. -->
    <style name="Theme.Opcode42" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">?android:attr/colorBackground</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>