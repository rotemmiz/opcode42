//
// Auth.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct Auth: Codable, JSONEncodable, Hashable {

    public enum ModelType: String, Codable, CaseIterable {
        case oauth = "oauth"
        case api = "api"
        case wellknown = "wellknown"
    }
    static let expiresRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public var access: String
    public var accountId: String?
    public var enterpriseUrl: String?
    public var expires: Int
    public var refresh: String
    public var type: ModelType
    public var key: String
    public var metadata: [String: String]?
    public var token: String

    public init(access: String, accountId: String? = nil, enterpriseUrl: String? = nil, expires: Int, refresh: String, type: ModelType, key: String, metadata: [String: String]? = nil, token: String) {
        self.access = access
        self.accountId = accountId
        self.enterpriseUrl = enterpriseUrl
        self.expires = expires
        self.refresh = refresh
        self.type = type
        self.key = key
        self.metadata = metadata
        self.token = token
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case access
        case accountId
        case enterpriseUrl
        case expires
        case refresh
        case type
        case key
        case metadata
        case token
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(access, forKey: .access)
        try container.encodeIfPresent(accountId, forKey: .accountId)
        try container.encodeIfPresent(enterpriseUrl, forKey: .enterpriseUrl)
        try container.encode(expires, forKey: .expires)
        try container.encode(refresh, forKey: .refresh)
        try container.encode(type, forKey: .type)
        try container.encode(key, forKey: .key)
        try container.encodeIfPresent(metadata, forKey: .metadata)
        try container.encode(token, forKey: .token)
    }
}

