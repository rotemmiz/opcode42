//
// AccountV2Credential.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct AccountV2Credential: Codable, JSONEncodable, Hashable {

    public enum ModelType: String, Codable, CaseIterable {
        case oauth = "oauth"
        case api = "api"
    }
    static let expiresRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public var access: String
    public var expires: Int
    public var refresh: String
    public var type: ModelType
    public var key: String
    public var metadata: [String: String]?

    public init(access: String, expires: Int, refresh: String, type: ModelType, key: String, metadata: [String: String]? = nil) {
        self.access = access
        self.expires = expires
        self.refresh = refresh
        self.type = type
        self.key = key
        self.metadata = metadata
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case access
        case expires
        case refresh
        case type
        case key
        case metadata
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(access, forKey: .access)
        try container.encode(expires, forKey: .expires)
        try container.encode(refresh, forKey: .refresh)
        try container.encode(type, forKey: .type)
        try container.encode(key, forKey: .key)
        try container.encodeIfPresent(metadata, forKey: .metadata)
    }
}

