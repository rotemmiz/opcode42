//
// ConfigMode.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ConfigMode: Codable, JSONEncodable, Hashable {

    public var build: AgentConfig?
    public var plan: AgentConfig?

    public init(build: AgentConfig? = nil, plan: AgentConfig? = nil) {
        self.build = build
        self.plan = plan
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case build
        case plan
    }

    public var additionalProperties: [String: AgentConfig] = [:]

    public subscript(key: String) -> AgentConfig? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(build, forKey: .build)
        try container.encodeIfPresent(plan, forKey: .plan)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        build = try container.decodeIfPresent(AgentConfig.self, forKey: .build)
        plan = try container.decodeIfPresent(AgentConfig.self, forKey: .plan)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("build")
        nonAdditionalPropertyKeys.insert("plan")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AgentConfig.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

