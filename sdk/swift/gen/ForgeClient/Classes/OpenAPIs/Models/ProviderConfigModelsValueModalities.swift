//
// ProviderConfigModelsValueModalities.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ProviderConfigModelsValueModalities: Codable, JSONEncodable, Hashable {

    public enum Input: String, Codable, CaseIterable {
        case text = "text"
        case audio = "audio"
        case image = "image"
        case video = "video"
        case pdf = "pdf"
    }
    public enum Output: String, Codable, CaseIterable {
        case text = "text"
        case audio = "audio"
        case image = "image"
        case video = "video"
        case pdf = "pdf"
    }
    public var input: [Input]?
    public var output: [Output]?

    public init(input: [Input]? = nil, output: [Output]? = nil) {
        self.input = input
        self.output = output
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case input
        case output
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(input, forKey: .input)
        try container.encodeIfPresent(output, forKey: .output)
    }
}

